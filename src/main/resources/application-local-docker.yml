spring:
  datasource:
    url: ${LOCAL_DB_URL}
    username: ${LOCAL_DB_USERNAME}
    password: ${LOCAL_DB_PASSWORD}

  sql:
    init:
      mode: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          time_zone: UTC
    defer-datasource-initialization: true

hirecore:
  cors:
    allowed-origins:
      - ${LOCAL_CORS_ALLOWED_ORIGIN}
    allowed-methods:
      - "GET"
      - "POST"
      - "PUT"
      - "PATCH"
      - "DELETE"
      - "OPTIONS"
    allowed-headers:
      - "*"
    allow-credentials: true

  cookie:
    http-only: true
    secure: false
    same-site: Lax

  jwt:
    secret: ${LOCAL_JWT_SECRET}
    access-expiration-millis: 10800000  # 임시 3시간 => 개발/운영 환경에서 변경 필요
    refresh-expiration-millis: 604800000 # 임시 7일 => 개발/운영 환경에서 변경 필요

  oauth2:
    kakao:
      client-redirect-url: ${LOCAL_OAUTH2_KAKAO_CLIENT_REDIRECT_URL}
      client-id: ${LOCAL_OAUTH2_KAKAO_CLIENT_ID}
      client-secret: ${LOCAL_OAUTH2_KAKAO_CLIENT_SECRET}
      grant-type: authorization_code
      token-issue-url: https://kauth.kakao.com/oauth/token
      user-profile-url: https://kapi.kakao.com/v2/user/me

  object-storage:
    region: ${LOCAL_OBJECT_STORAGE_REGION}
    bucket: ${LOCAL_OBJECT_STORAGE_BUCKET}
    endpoint: ${LOCAL_OBJECT_STORAGE_ENDPOINT}
    access-key: ${LOCAL_OBJECT_STORAGE_ACCESS_KEY}
    secret-key: ${LOCAL_OBJECT_STORAGE_SECRET_KEY}
    path-style-access: true